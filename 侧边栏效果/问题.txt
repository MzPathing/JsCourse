制作时，点击关闭按钮的时候，content部分会先隐藏再出来，与效果相反
原因：开启了一个定时器，一直设置left=0，导致虽然设置了left=-120px，但是由于定时器没有关闭，还是会再次更改为0，所以出现先到-120再到0的bug。、
解决方法：当定时器达到一定的时候，关闭定时器，否则，继续执行

ie8不支持console方法
解决方法：尽量在chrome或者火狐firebug中调试，找到错误的地方，更改后在ie中测试

ie8给未设置的属性添加值时，会报错（本案例为marginLeft）
解决方法：给所有要更改的值设置初始值（即使为0），防止报错